# Package Managers
package-lock.json
pnpm-lock.yaml
yarn.lock
bun.lock
bun.lockb

# Miscellaneous
/static/

# Mounted into the WebContainer and shown in the editor, so it keeps its own formatting
/webcontainer/
