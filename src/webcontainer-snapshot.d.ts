// Produced by the webcontainer-snapshots plugin in vite.config.ts, one export per directory
// under webcontainer/. Adding a resource type means adding a directory and a line here
declare module 'virtual:webcontainer-snapshots' {
	export const instanceGroup: Uint8Array;
	export const loadBalancer: Uint8Array;
}
